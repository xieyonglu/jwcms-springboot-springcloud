server:
  port: 8505


spring:
  application:
    name: spring-cloud-consul-consumer
  
  cloud:
    consul: # consul注册中心地址
      host: 127.0.0.1
      port: 8500
      discovery:
        service-name: ${spring.application.name}
